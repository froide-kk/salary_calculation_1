<%= form_with(model: add_employee, local: true) do |form| %>
  <% if add_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_employee.errors.count, "error") %> prohibited this add_employee from being saved:</h2>

      <ul>
      <% add_employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
