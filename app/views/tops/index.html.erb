<p id="notice"><%= notice %></p>

<h1>Result</h1>

<table>
  <thead>
    <tr>
      <th>Emp</th>
      <th>Name</th>
      <th>Age</th>
      <th>Pay</th>
	  <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tops.each do |top| %>
      <tr>
        <td><%= top.emp_id %></td>
        <td><%= top.name %></td>
        <td><%= top.age %></td>
        <td><%= top.pay %></td>
		<td><%= top.position %></td>
        <td><%= link_to 'Show', top %></td>
        <td><%= link_to 'Edit', edit_top_path(top) %></td>
        <td><%= link_to 'Destroy', top, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add Employees', new_top_path %>
